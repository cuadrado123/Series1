function accion(pag) {
    $('#tabAccion'  ).load(pag);
    }

function ficcion(pag) {
    $('#tabFiccion'  ).load(pag);
    }


function cargar(pag) {
    $('#tabComedia'  ).load(pag);
    }

function inicio(pag) {
    $('#tabInicio'  ).load(pag);
    }
